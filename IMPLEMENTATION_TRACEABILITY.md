# Implementation Traceability

Authoritative scope: local implementation specification supplied outside the repository. Status terms: `implemented`, `implemented-smoke-tested`, `empirically-pending`, or `not-applicable`.

| Requirement | Status | Files | Tests / Evidence | Blocker |
|---|---:|---|---|---|
| A. Causal event-time construction | implemented | `src/lob_forge/features.py`, `src/lob_forge/protocol.py` | `tests/test_features.py`, `tests/test_protocol.py`, `bash scripts/run_tests.sh` | None for local fixture path |
| B. Leakage-free preprocessing and sequence splits | implemented | `src/lob_forge/protocol.py`, `src/lob_forge/logistic.py`, `src/lob_forge/ml_models.py` | `tests/test_protocol.py`, `tests/test_ml_models.py` | None for local path |
| C. Strict model-selection protocol | implemented | `src/lob_forge/protocol.py`, `src/lob_forge/baselines.py`, `src/lob_forge/logistic.py`, `src/lob_forge/edge_model.py`, `src/lob_forge/cli.py` | `tests/test_protocol.py`, `tests/test_baselines.py`; search for CLI `test_*` sort choices | None |
| D. Real untouched calendar holdout | implemented-smoke-tested | `src/lob_forge/holdout.py`, `src/lob_forge/cli.py`, `scripts/run_reduced_e2e.py`, `scripts/prepare_final_holdout_from_expected_edge_study.sh` | `tests/test_holdout.py`, `tests/test_run_profiles.py`; reduced E2E declares holdout before selection and filters development rows; `final-holdout-rule` requires a frozen candidate pre-registered in the manifest; full-study final-holdout preparation is derived from `run_plan.json` and `candidate_registry.jsonl` | Final empirical holdout artifact pending a declared full run |
| E. Stateful execution simulator | implemented-smoke-tested | `src/lob_forge/execution_sim.py`, `src/lob_forge/portfolio.py`, `scripts/run_reduced_e2e.py` | `tests/test_execution_sim.py` covers post-only crossing rejection, same-timestamp passive causality, derived resting prices, and partial statuses; `tests/test_portfolio.py` covers exit-time realization, signed inventory, gross exposure, and symmetric shorts | Full strategy reports must be regenerated from simulator ledgers |
| F. Execution realism, capacity, adverse selection | implemented-smoke-tested | `src/lob_forge/execution_sim.py`, `src/lob_forge/fill_diagnostics.py`, `src/lob_forge/capacity.py`, `src/lob_forge/passive_capacity.py` | Maker classification now requires a nonmarketable order that existed before consuming trade flow; focused queue, liquidity, capacity, and adverse-selection tests pass | Paper/live fill validation remains external |
| G. Statistical validity | implemented-smoke-tested | `src/lob_forge/statistics.py`, `src/lob_forge/alpha_factory.py`, `src/lob_forge/experiment_registry.py`, `src/lob_forge/study_registry.py`, `src/lob_forge/study_provenance.py` | `tests/test_study_registry.py` proves one p-value per validation-selection procedure; `tests/test_study_status.py` rejects missing, duplicate, extra, or audit-mismatched procedure rows and stale/unprovenanced artifacts | Exact selective inference beyond the declared procedure-level audit remains a documented limitation |
| H. Baselines and neural models | implemented-smoke-tested | `src/lob_forge/baselines.py`, `src/lob_forge/logistic.py`, `src/lob_forge/edge_model.py`, `src/lob_forge/ml_models.py` | `tests/test_ml_models.py` proves chronological non-overlapping horizon exits, exact costs, zero residual inventory, hashed checkpoints/predictions, and a versioned frozen-candidate contract | Existing ignored Transformer/TCN result CSVs predate this contract and must be regenerated; large repeated-seed/GPU runs remain pending |
| I. Genuine L2 data path | implemented-smoke-tested | `src/lob_forge/data_sources.py`, `src/lob_forge/l2_ingest.py`, `src/lob_forge/l2_replay.py`, `src/lob_forge/l2_storage.py`, `examples/fixtures/l2_replay_fixture.csv` | `tests/test_data_sources.py`, `tests/test_l2_ingest.py`, `tests/test_l2_replay.py`, `tests/test_ml_models.py` | Multi-month real L2 coverage pending data acquisition |
| J. Research artifacts and reporting | implemented | `README.md`, `docs/research_note.md`, `docs/reproducibility.md`, `src/lob_forge/experiments.py`, `src/lob_forge/study_features.py`, `src/lob_forge/study_provenance.py`, `src/lob_forge/study_registry.py` | Daily source/config/output markers, ordered combined manifests, code/plan/holdout/result/audit sidecars, planned split checks, and fail-closed status tests | Capped and full empirical matrices must be regenerated under the new provenance contract |
| K. Repository quality and application readiness | implemented-smoke-tested | `.gitignore`, `.github/workflows/ci.yml`, `requirements-ci.txt`, docs | `bash scripts/run_tests.sh`; pytest, Ruff, mypy, shell syntax, and diff hygiene | None for local implementation checks |
| L. Focused C++ production signal | implemented-smoke-tested | `cpp/l2_replay.cpp`, `examples/fixtures/l2_replay_fixture.csv` | `tests/test_cpp_l2_replay.py`; compiles when `c++` is available | No Python extension needed for this scope |

## Verification Log

- Baseline direct tests before edits: `passed 160 direct test functions`.
- Current direct suite after remediation: `bash scripts/run_tests.sh` -> `passed 368 direct test functions`.
- Current pytest suite after remediation: `.venv/bin/python -m pytest -q` -> `368 passed`.
- Current static checks after implementation: `.venv/bin/python -m ruff format --check .`, `.venv/bin/python -m ruff check .`, and `.venv/bin/python -m mypy src/lob_forge` all passed.
- C++ replay equivalence is included in the direct suite when a local C++ compiler is present.
- Runbook holdout enforcement covers both Markdown code blocks and shell scripts, including multi-line Bash arrays that invoke gated research CLI commands.
- Reduced E2E fixture artifacts live under ignored `artifacts/reduced_e2e/`; the verifier now rejects both existing sequence smokes because they predate the versioned flat-at-horizon economics and baseline/checkpoint/prediction hash contract. Regenerate them only after these source changes are committed and the checkout is clean.
- Cloud handoff archive smoke-tested from an unpacked source-only ZIP without `.git`; `scripts/run_reduced_e2e.py` reads `.source-git-commit` and records `working_tree_dirty=null` instead of crashing, and `make verify-results` verifies the reduced E2E manifest against the archive commit while skipping absent full-study `results/current` only in that source-package mode.
- Cloud bootstrap and Modal full-study images install `requirements-research.txt` before installing the local package with `--no-deps`, so serious research runs use pinned dependency constraints rather than resolving optional dependency ranges from `pyproject.toml`. External readiness also rejects stale handoff ZIPs whose `.source-git-commit` does not match the current checkout commit.
- Sequence code/tests now enforce manifest-filtered development data, source/development-L2/baseline/checkpoint/prediction hashes, non-overlapping flat-at-label-horizon economics, zero residual inventory, exact resume configuration, checkpoint data/model/training contracts, a versioned `l2_sequence_torch_v2` candidate, and candidate-locked final evaluation. Existing local sequence CSVs are intentionally stale-contract and the dry-run runner schedules regeneration instead of silently skipping them.
- Safe full-study and live-validation paths were exercised in plan/dry-run mode. The plan-only, Kelly, and live-validation previews pass; the complete-feature and local16 existing-feature runners correctly exit nonzero because the current feature artifacts do not satisfy the content/configuration contract. The final-holdout preparation helper likewise correctly refuses the current checkout because the full study is incomplete.
- The core `results/current` artifact verifier still checks 15 files successfully, but aggregate `make verify-results` is intentionally red on 10 stale neural-contract errors. The remaining-evidence report is red for capped expected-edge provenance, full cloud evidence, immutable final holdout, real shadow fills, Kelly provenance, and sequence provenance/semantics; only the unaffected L2 pretraining smoke is green.
- The capped feature status reports `0/120` valid daily markers (`16` legacy-invalid, `104` missing), no combined feature manifests, `50/50` registry rows as `artifact_error`, and zero selected candidates. This replaces the previous false-green 20-fold headline with the actual regeneration requirement.
- `verify-results` now rejects unsupported audit `inference_grain` labels, requires present ledger artifacts before accepting trade/day/position ledger-grain inference claims, requires ordinary non-config-hash `pvalues.csv` files to cover the audit artifact family exactly, and rejects in-repository experiment ledgers whose `git_rev` does not match the current checkout `HEAD`, has no local tag, or whose `working_tree_dirty` provenance is missing, non-boolean, or true.

## Review Gap Status

This table maps the final "must be fixed before sending it" review items from `/Users/lemonnierhugo/.codex/attachments/597785c4-52c9-4068-95ba-a175387a43c2/pasted-text-1.txt` to current repository evidence.

| Review item | Current status | Evidence | Remaining blocker |
|---|---:|---|---|
| Rewrite `simulate_stateful_execution()` as a chronological event loop with globally consumed liquidity and adversarial tests | implemented-smoke-tested | `src/lob_forge/execution_sim.py`; `tests/test_execution_sim.py` covers global taker liquidity, post-only crossing rejection, same-event passive non-consumption (including between-event submit times), queueing, cancellation, expiry, rate limits, and kill switch behavior | Paper/live fill calibration remains external |
| Add `create-holdout-manifest` and propagate manifests through full-study scripts/runbooks | implemented-smoke-tested | `src/lob_forge/cli.py`; `scripts/holdout_manifest.sh`; `scripts/reproduce_core_results.sh`; `scripts/reproduce_multiday_results.sh`; `scripts/reproduce_result_artifacts.sh`; `scripts/run_60day_expected_edge_study.sh`; shell and Markdown runbook regressions in `tests/test_runbook_holdout_examples.py`; dry-run/plan commands listed above | Full cloud study still needs high-RAM execution |
| Replace reduced E2E placeholder statistics with OOS-only signals and ledger-derived statistics | implemented-smoke-tested | `scripts/run_reduced_e2e.py`; `_signals_from_walk_forward()` emits selected-rule test-window signals only; `_ledger_statistics_inputs()` derives net/gross PnL, returns, turnover, fees, and days from simulator ledgers | Synthetic fixture only; not empirical market evidence |
| Make final holdout use the stateful simulator and pre-register candidate hashes | implemented-smoke-tested | `final-holdout-rule`, `final-holdout-edge`, `final-holdout-sequence`, and `scripts/prepare_final_holdout_from_expected_edge_study.sh`; `tests/test_holdout.py` asserts pre-registered candidate hashes, duplicate-evaluation locks, and `stateful_simulator=true` for rule/edge results; `tests/test_run_profiles.py` asserts the full-study helper derives the development manifest, frozen edge candidate, candidate-locked final manifest, and final-holdout command | Full selected candidate final-holdout artifact has not been run |
| Expand the experiment registry beyond planned rows and record results/failures/selection | implemented-smoke-tested | `src/lob_forge/experiment_registry.py`, `src/lob_forge/study_registry.py`; invalid artifacts become `artifact_error` with no selected candidates, and inference is one row per complete validation-selection procedure | Capped and full matrices must be regenerated under the provenance contract |
| Complete the neural protocol before expensive hardware | implemented-smoke-tested | `src/lob_forge/ml_models.py`; `scripts/run_l2_sequence_experiments.sh`; tests cover versioned economics, hashes, frozen candidates, and final-holdout assertions | Existing local sequence artifacts must be regenerated after a provenance-valid baseline; large repeated-seed/GPU runs remain pending |
| Run simulated-vs-paper/live fill validation on real shadow or paper observations | empirically-pending | `scripts/prepare_shadow_fill_validation_session.sh`; `scripts/run_shadow_fill_observation_session.sh`; `submit-paper-orders`; provider order plans exist and the observation runner can submit, fetch order/fill history, normalize terminal zero-fill and positive-fill rows, import, and validate Bybit/OKX/Binance demo observations when credentials and demo/testnet order execution are available | Needs real observed demo/testnet fills |
| Fix full-research package dependency locking for cloud handoff and Modal runs | implemented-smoke-tested | `requirements-research.txt`; `scripts/bootstrap_cloud_expected_edge.sh`; `scripts/modal_expected_edge_job.py`; `scripts/package_cloud_handoff.sh`; `src/lob_forge/external_gate_readiness.py`; `docs/cloud_handoff.md`; `docs/reproducibility.md` | `tests/test_cloud_handoff.py::test_cloud_handoff_package_includes_locked_requirements`; `tests/test_external_gate_readiness.py::test_external_readiness_rejects_stale_cloud_package_source_commit`; focused shell syntax and ruff checks | Normal local editable installs remain range-based by design |

## Empirical Work Still Pending

- Provenance-valid capped BTC/ETH regeneration with full planned daily coverage.
- Full multi-month, multi-asset confirmatory run.
- Immutable final holdout evaluation artifact.
- Genuine crypto L2 neural experiments over sufficient data.
- Large repeated-seed stochastic model sweeps over genuine L2 data.
- Paper/live fill validation against observed fills.
